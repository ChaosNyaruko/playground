#include "stdc++.h"

inline void print(std ::vector<int> &&vec) {
  for (auto &v : vec) {
    printf("%d\n", v);
  }
}

inline void print(std ::vector<std::string> &&vec) {
  for (auto &v : vec) {
    printf("%s\n", v.c_str());
  }
}
