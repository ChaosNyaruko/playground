
double add(const vector<double> &v) {
  double res = 0;
  int nCPU = m;
  int n = v.size();
  res[m]
  for i in m {
    res = sum(v[i*ceiling(n/m):i*ceiling(n/m)+ceiling(n/m)])
  }
  m = 8
    n = 1000
  v[0...125][1
  return sum(res);
  for (auto &x : v) {
    res += x;
  }
  return res;
}
